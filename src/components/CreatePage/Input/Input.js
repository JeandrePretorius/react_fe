import React from 'react';
import PropTypes from 'prop-types';
import './Input.css';

const Input = () => (
  <div className="Input">
    Input Component
  </div>
);

Input.propTypes = {};

Input.defaultProps = {};

export default Input;
